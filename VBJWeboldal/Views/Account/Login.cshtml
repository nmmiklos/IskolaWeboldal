@model VBJWeboldal.ViewModels.LoginViewModel
@using Microsoft.Extensions.Configuration
@inject IConfiguration Configuration
@{
    ViewData["Title"] = "Bejelentkezés";
}
<script src="https://www.google.com/recaptcha/api.js" async defer></script>

<div class="section">
    <div class="section-h">
        <h3>Bejelentkezés</h3>
        <p>Lépj be a szerkesztői felülethez</p>
    </div>

    @* @if (User.Identity != null && User.Identity.IsAuthenticated)
    {
        <div style="max-width: 500px; margin: 40px auto; text-align: center; background: var(--card); padding: 30px; border-radius: 12px; box-shadow: var(--shadow);">
            <h2 style="color: var(--text-strong); margin-bottom: 20px;">Hoppá, te már be vagy jelentkezve!</h2>

            <img src="https://media.giphy.com/media/njYrp176NQsHS/giphy.gif" alt="You shall not pass" style="width: 100%; border-radius: 8px; box-shadow: 0 4px 10px rgba(0,0,0,0.2);" />

            <div style="margin-top: 25px;">
                <a asp-action="Index" asp-controller="Admin" class="btn" style="background: var(--accent); color:#2b240d; text-decoration: none; padding: 10px 20px; border-radius: 4px; display: inline-block; font-weight: bold;">
                    Tovább az Admin felületre 🪄
                </a>
            </div>
        </div>
    }
    else
    { *@
        <div style="max-width: 400px; margin: 0 auto; padding: 20px; background: var(--card); border-radius: 8px; box-shadow: var(--shadow); color: var(--text);">
            <form asp-action="Login" method="post">
                <div asp-validation-summary="ModelOnly" style="color: red; margin-bottom: 10px;"></div>

                <div style="margin-bottom: 15px;">
                    <label asp-for="Email" style="font-weight: bold;"></label><br />
                    <input asp-for="Email" style="width: 100%; padding: 10px; border: 1px solid var(--line); border-radius: 4px; background: var(--card); color: var(--text);" />
                    <span asp-validation-for="Email" style="color: red; font-size: 0.85em;"></span>
                </div>

                <div style="margin-bottom: 15px;">
                    <label asp-for="Password" style="font-weight: bold;">Jelszó</label><br />
                    <input asp-for="Password" style="width: 100%; padding: 10px; border: 1px solid var(--line); border-radius: 4px; background: var(--card); color: var(--text);" />
                    <span asp-validation-for="Password" style="color: red; font-size: 0.85em;"></span>
                </div>

               @*  <div style="margin-bottom: 15px;">
                    <div class="g-recaptcha" data-sitekey="@Configuration["ReCaptcha:SiteKey"]"></div>
                </div> *@

                <button type="submit" class="btn" style="background: var(--accent); color:#2b240d; width: 100%;">Bejelentkezés</button>
            </form>
        </div>
    @* } *@
</div>